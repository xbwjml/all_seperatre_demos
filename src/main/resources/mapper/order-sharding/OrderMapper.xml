<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.ShardingSphereCases.orderCase.mapper.OrderMapper">

    <!--
      与 t_order 物理表一一对应的字段映射。
      Configuration 已开启 mapUnderscoreToCamelCase, 一般无需手写 ResultMap;
      但 status 是 primitive byte / payType 是 Byte 包装, 显式声明可避免类型推断踩坑。
    -->
    <resultMap id="OrderResultMap" type="com.example.demo.ShardingSphereCases.orderCase.domain.Order">
        <id     property="orderId"     column="order_id"      jdbcType="BIGINT"/>
        <result property="orderNo"     column="order_no"      jdbcType="VARCHAR"/>
        <result property="userId"      column="user_id"       jdbcType="BIGINT"/>
        <result property="sellerId"    column="seller_id"     jdbcType="BIGINT"/>
        <result property="totalAmount" column="total_amount"  jdbcType="DECIMAL"/>
        <result property="payAmount"   column="pay_amount"    jdbcType="DECIMAL"/>
        <result property="status"      column="status"        jdbcType="TINYINT"/>
        <result property="payType"     column="pay_type"      jdbcType="TINYINT"/>
        <result property="createTime"  column="create_time"   jdbcType="TIMESTAMP"/>
        <result property="updateTime"  column="update_time"   jdbcType="TIMESTAMP"/>
        <result property="payTime"     column="pay_time"      jdbcType="TIMESTAMP"/>
        <result property="remark"      column="remark"        jdbcType="VARCHAR"/>
        <result property="version"     column="version"       jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        order_id, order_no, user_id, seller_id, total_amount, pay_amount,
        status, pay_type, create_time, update_time, pay_time, remark, version
    </sql>

    <insert id="insert" parameterType="com.example.demo.ShardingSphereCases.orderCase.domain.Order">
        INSERT INTO t_order (
            order_id, order_no, user_id, seller_id,
            total_amount, pay_amount, status, pay_type,
            create_time, update_time, pay_time, remark, version
        ) VALUES (
            #{orderId,jdbcType=BIGINT},
            #{orderNo,jdbcType=VARCHAR},
            #{userId,jdbcType=BIGINT},
            #{sellerId,jdbcType=BIGINT},
            #{totalAmount,jdbcType=DECIMAL},
            #{payAmount,jdbcType=DECIMAL},
            #{status,jdbcType=TINYINT},
            #{payType,jdbcType=TINYINT},
            #{createTime,jdbcType=TIMESTAMP},
            #{updateTime,jdbcType=TIMESTAMP},
            #{payTime,jdbcType=TIMESTAMP},
            #{remark,jdbcType=VARCHAR},
            #{version,jdbcType=INTEGER}
        )
    </insert>

    <!-- 用户维度精确查询: user_id 命中分片键, 单分片单表 -->
    <select id="selectByUserAndOrderId" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE user_id = #{userId,jdbcType=BIGINT}
          AND order_id = #{orderId,jdbcType=BIGINT}
        LIMIT 1
    </select>

    <!--
      仅凭 order_id 查询: WHERE 不含分片键 user_id, ShardingSphere 广播到所有分片归并。
      order_id 全局唯一(uk_order_id), 至多命中一行。
    -->
    <select id="selectByOrderId" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE order_id = #{orderId,jdbcType=BIGINT}
        LIMIT 1
    </select>

    <!-- 用户订单列表, 基于 order_id 游标降序分页 -->
    <select id="listByUser" resultMap="OrderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE user_id = #{userId,jdbcType=BIGINT}
          AND order_id &lt; #{lastOrderId,jdbcType=BIGINT}
        ORDER BY order_id DESC
        LIMIT #{size}
    </select>

    <!--
      状态更新 + 乐观锁 + 状态机校验。
      只有在 (user_id, order_id, status, version) 全匹配时才会更新, 否则受影响行数 = 0。
      payTime 仅在转入"已支付"时由调用方传入。
    -->
    <update id="updateStatus">
        UPDATE t_order
        SET status = #{targetStatus,jdbcType=TINYINT},
            version = version + 1,
            update_time = NOW(3)
            <if test="payTime != null">
                , pay_time = #{payTime,jdbcType=TIMESTAMP}
            </if>
        WHERE user_id = #{userId,jdbcType=BIGINT}
          AND order_id = #{orderId,jdbcType=BIGINT}
          AND status = #{expectedFromStatus,jdbcType=TINYINT}
          AND version = #{expectedVersion,jdbcType=INTEGER}
    </update>

</mapper>
